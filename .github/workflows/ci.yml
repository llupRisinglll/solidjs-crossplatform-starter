name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check
      - run: npm run test:build

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: PLATFORM=web PRESET=static npm run build
      - run: npm run test:e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/

  # Uncomment to build desktop artifacts on tagged releases:
  # desktop:
  #   if: startsWith(github.ref, 'refs/tags/v')
  #   strategy:
  #     matrix:
  #       include:
  #         - os: ubuntu-latest
  #           target: linux
  #         - os: macos-latest
  #           target: macos
  #         - os: windows-latest
  #           target: windows
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #     - uses: dtolnay/rust-toolchain@stable
  #     - run: npm ci
  #     - run: npm run build:desktop
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: desktop-${{ matrix.target }}
  #         path: src-tauri/target/release/bundle/
