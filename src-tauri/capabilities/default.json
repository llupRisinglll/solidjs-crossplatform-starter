{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the starter. Add more as your app needs them.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "shell:default",
    "opener:default",
    "deep-link:default",
    "window-state:default"
  ]
}
